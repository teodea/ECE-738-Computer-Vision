image_path = '../images/ReconstructionData/L_rectified_stereoU1.jpg';
number_of_strongest_features = 100;

sift_features  = sift(image_path, number_of_strongest_features);
non_max_suppression(features)

features = surf(image_path, number_of_strongest_features);
non_max_suppression(features)

features = harris(image_path, number_of_strongest_features);
non_max_suppression(features)